# Copy to ".env" (local) or set these as environment variables on your host.
# These seed the FIRST moderator account on first run — change them before going live.

ADMIN_USER=moderator
ADMIN_PASS=change-this-to-a-strong-password

# Port is set automatically by most hosts; only override locally if needed.
# PORT=3000
